<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Chaotic veneers over infrastructure clients for .NET tests. Build an HttpClient (or any interface) that follows a timeline of faults: freeze for 10 seconds, fail the next 3 calls, add latency every 30 seconds.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ChaosDotNet.Tests" />
    <PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" />
    <PackageReference Include="Microsoft.Extensions.Http" />
  </ItemGroup>

</Project>
