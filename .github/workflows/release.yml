name: Release

on:
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Version from tag
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Build
        run: dotnet build --configuration Release -p:Version=$VERSION

      - name: Test
        run: dotnet test --solution ChaosDotNet.slnx --no-build --configuration Release

      - name: Pack
        run: dotnet pack --no-build --configuration Release -p:Version=$VERSION --output artifacts

      # Trusted Publishing: nuget.org trusts this workflow, so no API key is stored in the repo.
      - name: NuGet login
        uses: NuGet/login@v1
        id: login
        with:
          user: webuild

      - name: Push to NuGet
        run: dotnet nuget push "artifacts/*.nupkg" --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate
